---
phase: 03-timeline-modes
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/Index.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Deals.tsx
  - src/components/InvestModal.tsx
autonomous: false
requirements:
  - TIME-01
  - TIME-02

must_haves:
  truths:
    - "Fund setup wizard shows IRL vs Freeplay toggle as a dedicated step before fund type selection"
    - "Selecting IRL mode and launching a fund results in fund.timelineMode being 'irl' in the store"
    - "Dashboard displays a visible IRL Pacing badge when fund is in IRL mode"
    - "In IRL mode, gated deal actions show 'Available in N months' and are disabled"
    - "In Freeplay mode, no gate indicators appear and all actions are immediately available"
  artifacts:
    - path: "src/pages/Index.tsx"
      provides: "Timeline mode wizard step with IRL/Freeplay cards, timelineMode passed to initFund"
      contains: "timelineMode"
    - path: "src/pages/Dashboard.tsx"
      provides: "IRL Pacing badge in header area"
      contains: "IRL Pacing"
    - path: "src/pages/Deals.tsx"
      provides: "Gate status display on deal actions"
      contains: "checkTimeGate"
    - path: "src/components/InvestModal.tsx"
      provides: "Gate check on invest button with countdown label"
      contains: "checkTimeGate"
  key_links:
    - from: "src/pages/Index.tsx"
      to: "initFund"
      via: "timelineMode passed in config object"
      pattern: "timelineMode"
    - from: "src/pages/Deals.tsx"
      to: "src/engine/timelineGates.ts"
      via: "import checkTimeGate for UI gate display"
      pattern: "import.*checkTimeGate.*from.*timelineGates"
    - from: "src/components/InvestModal.tsx"
      to: "src/engine/timelineGates.ts"
      via: "import checkTimeGate for invest button disable"
      pattern: "import.*checkTimeGate.*from.*timelineGates"
---

<objective>
Add the timeline mode selection to the fund setup wizard and surface gate status in the game UI.

Purpose: Complete the player-facing experience — users choose their pacing mode at fund creation, see a mode indicator on the Dashboard, and encounter "available in N months" on gated actions in IRL mode. Freeplay users see no difference from current behavior.

Output: Modified Index.tsx (6-step wizard with mode selection), Dashboard.tsx (IRL badge), Deals.tsx and InvestModal.tsx (gate status display).
</objective>

<execution_context>
@/Users/soorajsambasivam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/soorajsambasivam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-modes/03-RESEARCH.md
@.planning/phases/03-timeline-modes/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — these will exist when Plan 02 executes -->

From src/engine/timelineGates.ts:

```typescript
export const IRL_GATE_DURATIONS: IrlGateDurations;
export function checkTimeGate(
  fund: Fund,
  actionType: string,
): {
  blocked: boolean;
  availableFromMonth?: number;
  monthsRemaining?: number;
};
export function openTimeGate(
  fund: Fund,
  actionType: string,
  reason: string,
): TimeGate;
export function clearExpiredGates(fund: Fund): TimeGate[];
```

From src/engine/types.ts:

```typescript
export type TimelineMode = "irl" | "freeplay";
```

From src/pages/Index.tsx (existing wizard pattern):

- 5-step wizard: [1] name, [2] fund type/stage/geo, [3] fund size, [4] scenario, [5] era + fundraising
- Step progress rendered via `[1, 2, 3, 4, 5].map(...)` hardcoded array
- Local state: `const [step, setStep] = useState(1)`
- `launchFund()` calls `initFund({ name, type, stage, targetSize, currentSize, skillLevel, rebirthCount, geographicFocus, scenarioId, marketEra })`

From src/pages/Dashboard.tsx (existing badge pattern):

- Scenario banner already rendered conditionally: `fund.scenarioId && <Badge>...</Badge>`
- Clock icon available from Lucide React
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeline mode wizard step to Index.tsx</name>
  <files>src/pages/Index.tsx</files>
  <action>
Modify `src/pages/Index.tsx` to add a timeline mode selection step as the new Step 2, pushing all existing steps to 3-6 (total: 6 steps).

1. Add state: `const [timelineMode, setTimelineMode] = useState<TimelineMode>("freeplay")` — import `TimelineMode` from `@/engine/types`.

2. Update the step progress indicator: change `[1, 2, 3, 4, 5].map(...)` to `[1, 2, 3, 4, 5, 6].map(...)`.

3. Renumber all existing step references:
   - Old Step 2 (fund type/stage/geo) becomes Step 3
   - Old Step 3 (fund size) becomes Step 4
   - Old Step 4 (scenario) becomes Step 5
   - Old Step 5 (era + fundraising) becomes Step 6
   - Update all `setStep(N)` calls, all `step === N` conditionals, and all "Back"/"Continue" button onClick handlers accordingly.

4. Add new Step 2 content — two selectable cards (same pattern as fund type selection in current Step 2):
   - **Freeplay** card: title "Freeplay", description "No time constraints — invest, pitch LPs, and advance at your own pace. Best for learning.", icon: Zap from lucide-react.
   - **IRL Mode** card: title "IRL Pacing", description "Actions take realistic time — seed checks need 1-2 months, due diligence 1-3 months. Feel the real rhythm of VC.", icon: Clock from lucide-react.
   - Selected card gets `border-primary` ring, unselected gets `border-border`. Use the same card selection pattern already used for fund type selection.
   - "Back" returns to Step 1, "Continue" advances to Step 3. Freeplay is pre-selected so Continue is always enabled.

5. In `launchFund()`, add `timelineMode` to the config object passed to `initFund()`:
   ```typescript
   initFund({
     ...existingFields,
     timelineMode,
   } as Parameters<typeof initFund>[0]);
   ```

Use `t()` from `@/lib/i18n` for all new user-facing strings.
</action>
<verify>
<automated>npx tsc -b --noEmit && npx vitest run</automated>
</verify>
<done>Wizard has 6 steps with Step 2 showing IRL/Freeplay selection. timelineMode is passed through to initFund. All step navigation works correctly. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add IRL badge on Dashboard and gate display on Deals/InvestModal</name>
  <files>src/pages/Dashboard.tsx, src/pages/Deals.tsx, src/components/InvestModal.tsx</files>
  <action>
Three UI surfaces need gate awareness:

**Dashboard.tsx — IRL Mode Badge:**

1. Import `Clock` from lucide-react (if not already imported).
2. Near the scenario banner area (already conditionally rendered), add:
   ```tsx
   {
     fund.timelineMode === "irl" && (
       <Badge variant="outline" className="gap-1">
         <Clock className="h-3 w-3" /> {t("dashboard.irlPacing", "IRL Pacing")}
       </Badge>
     );
   }
   ```
   Place it adjacent to the existing scenario badge so both show when applicable.

**Deals.tsx — Gate Status on Deal Cards:**

1. Import `checkTimeGate` from `@/engine/timelineGates`.
2. For each deal card's invest action area, determine the gate key based on the startup's stage: `"pre_seed"` or `"seed"` -> `"seed_check"`, otherwise -> `"due_diligence"`.
3. Call `const gateCheck = checkTimeGate(fund, gateKey)`.
4. If `gateCheck.blocked`, render a small `Badge` with text like `"Available in {gateCheck.monthsRemaining}mo"` using `variant="secondary"` and disable the invest button for that card.
5. In freeplay mode, `checkTimeGate` returns `{ blocked: false }` so no badge renders — zero changes to freeplay UI.

**InvestModal.tsx — Gate Check on Confirm Button:**

1. Import `checkTimeGate` from `@/engine/timelineGates`.
2. Read `fund` from `useGameStore`.
3. Determine gate key from the startup being invested in (same stage logic as Deals.tsx).
4. Call `checkTimeGate(fund, gateKey)`. If blocked, disable the confirm button and show the countdown message below it: `t("timeGate.availableIn", \`Available in ${gateCheck.monthsRemaining} month(s)\`)`.
5. This is a safety net — the store action also rejects. But the UI should prevent the attempt visually.

Use `t()` from `@/lib/i18n` for all new user-facing strings. Do not re-implement gate logic — always import `checkTimeGate` from the engine module.
</action>
<verify>
<automated>npx tsc -b --noEmit && npx vitest run</automated>
</verify>
<done>Dashboard shows IRL Pacing badge when fund.timelineMode is "irl". Deals page shows "Available in N mo" badge on gated deals with disabled invest button. InvestModal disables confirm when gate is active. No visual changes in freeplay mode. TypeScript compiles clean, all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify timeline modes end-to-end</name>
  <files>none</files>
  <action>
Human visual verification of the complete timeline modes feature across wizard, Dashboard, and Deals.
  </action>
  <verify>Manual visual inspection per steps below</verify>
  <done>User confirms wizard toggle, IRL badge, and gate display all work correctly in both IRL and Freeplay modes</done>
  <what-built>Complete timeline modes feature: wizard selection (Step 2), IRL badge on Dashboard, gate display on Deals and InvestModal</what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to http://localhost:5173
    2. Start a new fund — verify Step 2 shows IRL/Freeplay toggle cards
    3. Select "IRL Pacing", complete wizard, launch fund
    4. On Dashboard, verify "IRL Pacing" badge appears near fund info
    5. Navigate to Deals — if any gates are active, verify "Available in N mo" badge appears
    6. Click an invest button — verify InvestModal shows gate status if blocked
    7. Reset and start a Freeplay fund — verify no IRL badge on Dashboard and no gate indicators on Deals
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
npx tsc -b --noEmit && npx vitest run
All tests pass. TypeScript strict mode clean. No regressions.
</verification>

<success_criteria>

- Fund setup wizard has 6 steps with IRL/Freeplay selection as Step 2
- Selecting IRL mode results in fund.timelineMode === "irl" in the store
- Dashboard displays IRL Pacing badge in IRL mode, hidden in Freeplay
- Deals page shows gate countdown on blocked actions in IRL mode
- InvestModal disables confirm button when gate is active
- Freeplay mode shows zero gate-related UI
- All tests pass, tsc -b clean
  </success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-modes/03-02-SUMMARY.md`
</output>
