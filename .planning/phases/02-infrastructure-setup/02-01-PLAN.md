---
phase: 02-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/lib/i18n.test.ts
  - src/App.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "t('key', 'fallback') returns 'fallback'"
    - "t('key') with no fallback returns 'key'"
    - "t() is a plain function importable without React context"
    - "SpeedInsights component renders inside BrowserRouter in App.tsx"
    - "Build passes tsc -b with zero errors after additions"
    - "Bundle size delta is under 15 KB"
  artifacts:
    - path: "src/lib/i18n.ts"
      provides: "i18n shim t() function"
      exports: ["t"]
      min_lines: 5
    - path: "src/lib/i18n.test.ts"
      provides: "Unit tests for t() shim"
      min_lines: 10
    - path: "src/App.tsx"
      provides: "SpeedInsights component rendered"
      contains: "SpeedInsights"
  key_links:
    - from: "src/lib/i18n.ts"
      to: "future phase JSX files"
      via: "import { t } from '@/lib/i18n'"
      pattern: "export function t"
    - from: "src/App.tsx"
      to: "@vercel/speed-insights/react"
      via: "import and JSX render"
      pattern: "SpeedInsights"
---

<objective>
Install the two cross-cutting utilities required before any new UI is written: the i18n shim (`t()` function) and Vercel Speed Insights.

Purpose: All subsequent phases (3-8) will import `t()` for user-facing strings. Speed Insights starts collecting Core Web Vitals on deploy.
Output: `src/lib/i18n.ts`, `src/lib/i18n.test.ts`, updated `src/App.tsx` with `<SpeedInsights />`.
</objective>

<execution_context>
@/Users/soorajsambasivam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/soorajsambasivam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-setup/02-RESEARCH.md
@.planning/phases/02-infrastructure-setup/02-VALIDATION.md

<interfaces>
<!-- Executor needs to know App.tsx structure for SpeedInsights placement -->

From src/App.tsx (lines 71-74, placement zone):

```tsx
        <KeyboardShortcuts />
        <Toaster />
      </TooltipProvider>
    </BrowserRouter>
```

From src/lib/utils.ts (style reference — match this file's conventions):

```typescript
// Simple exported functions, no default exports, no React imports
export function cn(...inputs: ClassValue[]) { ... }
export function formatCurrency(value: number): string { ... }
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create i18n shim with tests</name>
  <files>src/lib/i18n.ts, src/lib/i18n.test.ts</files>
  <behavior>
    - t('dashboard.title', 'Dashboard') returns 'Dashboard'
    - t('nav.deals', 'Deals') returns 'Deals'
    - t('some.key') with no fallback returns 'some.key'
    - t() is a plain exported function with zero imports (no React, no dependencies)
  </behavior>
  <action>
1. Create `src/lib/i18n.test.ts` first (RED phase):
   - Import `t` from `@/lib/i18n`
   - Test: `t('dashboard.title', 'Dashboard')` returns `'Dashboard'`
   - Test: `t('nav.deals', 'Deals')` returns `'Deals'`
   - Test: `t('some.key')` (no fallback) returns `'some.key'`
   - Run tests — they MUST fail (module not found)

2. Create `src/lib/i18n.ts` (GREEN phase):
   - Export a single function: `export function t(key: string, fallback?: string): string`
   - Implementation: `return fallback ?? key;`
   - Add JSDoc explaining this is a v1 shim, v2 replaces with react-i18next
   - Use dot-namespaced key convention in JSDoc example: `t('nav.deals', 'Deals')`
   - Zero imports — no React, no dependencies
   - Run tests — they MUST pass

IMPORTANT: Do NOT wrap any existing strings in existing components. The requirement is "all NEW user-facing strings in subsequent phases use it" — not retroactive.
</action>
<verify>
<automated>npx vitest run src/lib/i18n.test.ts</automated>
</verify>
<done> - `src/lib/i18n.ts` exists with exported `t()` function - `src/lib/i18n.test.ts` exists with 3 passing tests - `t('key', 'fallback')` returns `'fallback'`; `t('key')` returns `'key'` - File has zero imports (plain function, no React context needed)
</done>
</task>

<task type="auto">
  <name>Task 2: Install Speed Insights and wire into App.tsx</name>
  <files>src/App.tsx, package.json, package-lock.json</files>
  <action>
1. Install the package:
   ```bash
   npm install @vercel/speed-insights
   ```

2. Edit `src/App.tsx`:
   - Add import: `import { SpeedInsights } from '@vercel/speed-insights/react';`
   - IMPORTANT: Use `@vercel/speed-insights/react` (NOT the root path `@vercel/speed-insights`)
   - Add `<SpeedInsights />` as the last child inside `<BrowserRouter>`, after `<Toaster />`:
     ```tsx
     <KeyboardShortcuts />
     <Toaster />
     <SpeedInsights />
     ```
   - No props needed — defaults are correct for this project

3. Verify:
   - Run `npx tsc -b --noEmit` — must pass clean
   - Run `npm run build` — must succeed; check Vite output to confirm no new chunk > 15 KB for speed-insights
   - Run `npm test` — all 173 existing tests must still pass
     </action>
     <verify>
     <automated>npx tsc -b --noEmit && npm run build && npm test</automated>
     </verify>
     <done>
   - `@vercel/speed-insights` is in package.json dependencies
   - `App.tsx` imports `SpeedInsights` from `@vercel/speed-insights/react`
   - `<SpeedInsights />` renders inside `<BrowserRouter>` after `<Toaster />`
   - `tsc -b` passes clean, build succeeds, all existing tests pass
   - Bundle delta under 15 KB (verify from Vite build output)
     </done>
     </task>

</tasks>

<verification>
Phase-level verification after both tasks complete:

1. `npx tsc -b --noEmit` — zero TypeScript errors
2. `npm test` — all tests pass (173 existing + 3 new i18n tests)
3. `npm run build` — succeeds with bundle delta < 15 KB
4. `grep -r "SpeedInsights" src/App.tsx` — confirms component is rendered
5. `grep -r "export function t" src/lib/i18n.ts` — confirms shim exists
   </verification>

<success_criteria>

- i18n shim `t()` is importable from `@/lib/i18n` as a plain function (no React context)
- `t('key', 'English')` returns `'English'`; `t('key')` returns `'key'`
- 3 unit tests for `t()` pass in `src/lib/i18n.test.ts`
- `<SpeedInsights />` is rendered inside `<BrowserRouter>` in `App.tsx`
- `tsc -b` clean, `npm test` green, `npm run build` succeeds
- Bundle delta < 15 KB
- No existing files modified except `src/App.tsx` (and `package.json`/`package-lock.json` for the install)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-setup/02-01-SUMMARY.md`
</output>
